/**
 * The core module.
 *
 * Provides the framework independent functions and classes.
 *
 * @module core
 * @main core
 */
import * as Core        from './scroll/core';
import * as BaseEffects from './scroll/base-effects';

export const scroll = {
  Core,
  BaseEffects,
};
